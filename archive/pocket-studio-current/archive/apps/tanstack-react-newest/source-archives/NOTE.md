# Archive provenance

- `pocket-studio-booking-10a2c4c2-variant/` — working tree of the stray clone at
  `/home/mykey/pocket-studio-booking-10a2c4c2` (commit `8084178` "merge booking sites into
  multi-route Pocket Studio app", plus its uncommitted changes: a server-side
  `/api/travel-fee` prototype in `src/routes/book.tsx` + `travel` constants in
  `src/content.ts`). That commit is not in this repo's history; the prototype is
  superseded by the client-side `src/components/TravelFee.tsx`. Archived 2026-07-28
  (`.git`, `node_modules`, `.env`, build output excluded). Its `booking-archive/`
  subfolder holds the older static/spa variants (booking-page, mykey-booking-site, etc.).
- `booking-page-legacy/` — the standalone static `index.html` from `/home/mykey/booking-page`
  (commit `f5ee304` "Set booking request email"), an early single-file version.
